# ==============================================================
# ProfileImg-Plugin 配置文件
# ==============================================================
gallery:
  # ---------- 主图库仓库元数据 ----------
  # 仓库列表由 map.json 的 mapping values 自动发现（仓库 0 始终活跃）
  # 此处仅提供各仓库的元数据补丁（URL / cron / 开关），未配置的仓库使用默认值
  repos:
    - id: 0
      name: "miao-plugin-ProfileImg"
      remoteUrl: "https://github.com/AxiuCN/miao-plugin-ProfileImg.git"
      enabled: true
      cron: "0 20 5 * * *"
      autoUpdate: true
      autoRestart: false

  # ---------- default / 第三方图库 ----------
  # 见 config/gallery_config.yaml（default 图库路径、第三方图库列表）
  # default.dir 也可沿用本文件旧字段 gallery.defaultDir（兼容）

  # ---------- 屏蔽图库 ----------
  blocked:
    enabled: true
    remoteUrl: "https://github.com/AxiuCN/miao-plugin-ProfileImg-Blocked.git"
    cron: "0 40 5 * * *"
    autoUpdate: true
    autoRestart: false

# ---------- 上传压缩设置 ----------
upload:
  enabled: false
  format: "webp"
  maxSize: 500
