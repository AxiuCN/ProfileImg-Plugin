# ==============================================================
# ProfileImg-Plugin 成员管理权限配置
# 允许以下群成员执行管理指令：添加、删除、屏蔽、启用
# 成员只能操作被允许图库内的图（主人不受限）
# ==============================================================

# ---------- 允许使用管理指令的群成员 ----------
# qq: 群成员 QQ 号
# repos: 允许操作的图库类型（逗号分隔，可多个）
#   main      → 主图库（一体）
#   default   → default 图库
#   第三方图库名 → 如 米游社（对应 gallery_config.yaml 的 thirdParty[].name）
# 留空 = 仅允许 default 图库
managers: []
# 添加成员示例（取消注释）：
# - qq: 123456789
#   repos: "main,default,米游社"
