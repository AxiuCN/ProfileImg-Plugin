# ==============================================================
# ProfileImg-Plugin 成员管理权限配置
# 允许以下群成员执行管理指令：添加、删除、屏蔽、启用
# 每个成员只能操作归属其管理仓库内的角色（主人不受限）
# ==============================================================

# ---------- 允许使用管理指令的群成员 ----------
# qq: 群成员 QQ 号
# repoId: 管理仓库编号（可选，留空 = 默认仓库 0，即 miao-plugin-ProfileImg）
#   成员上传新角色时分配到该仓库；已有角色不属于该仓库则拒绝操作
managers: []
# 添加成员示例（取消注释）：
# - qq: 123456789
#   repoId: 0
